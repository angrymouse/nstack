{
  "name": "nstack",
  "version": "0.1.49",
  "private": true,
  "type": "module",
  "bin": {
    "nstack": "./bin/nstack.js"
  },
  "scripts": {
    "bench:heavy": "node scripts/bench-heavy.mjs",
    "check": "node --check bin/nstack.js && find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "smoke": "node scripts/smoke.mjs",
    "test": "node --import ./scripts/temp-cleanup-preload.mjs --test"
  },
  "dependencies": {
    "@clack/prompts": "^1.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
