{
  "name": "nstack",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "bin": {
    "nstack": "./bin/nstack.js"
  },
  "scripts": {
    "bench:heavy": "node scripts/bench-heavy.mjs",
    "check": "node --check bin/nstack.js && find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "smoke": "node bin/nstack.js help && node bin/nstack.js init /tmp/nstack-smoke --force --yes && node --check /tmp/nstack-smoke/nstack.config.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@clack/prompts": "^1.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
